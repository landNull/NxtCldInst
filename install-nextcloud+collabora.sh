#!/bin/sh
# INCOMPLETE on GitHub — this session could not upload the 46k body.
# Full script: artifacts/install-nextcloud-collabora.sh
# (same file as install-nextcloud+collabora.sh; + is awkward on some mounts)
echo "$0: placeholder only. Copy artifacts/install-nextcloud-collabora.sh over this file and push." >&2
exit 1
